part of 'loginscreen_bloc.dart';

class LoginscreenState {
  final bool isloading;

  LoginscreenState(this.isloading);
}

class LoginscreenInitial extends LoginscreenState {
  LoginscreenInitial(super.isloading);
}
